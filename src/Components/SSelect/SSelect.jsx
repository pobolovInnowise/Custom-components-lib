import * as React from 'react';
import styles from './SSelect.module.css'

function SSelect(props) {
    const clazz = ``;

    return (
        <select/>
    );
}

export default SSelect;


// пример от чата гпт
// function TSelect({ options, value, onChange, label, disabled }) {
//     return (
//         <label>
//             {label}
//             <select value={value} onChange={onChange} disabled={disabled}>
//                 {options.map((opt) => (
//                     <option key={opt.value} value={opt.value}>
//                         {opt.label}
//                     </option>
//                 ))}
//             </select>
//         </label>
//     );
// }
